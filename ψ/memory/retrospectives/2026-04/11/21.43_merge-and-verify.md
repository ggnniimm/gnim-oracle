---
session: d73a673a
date: 2026-04-11
time: 21:43 GMT+7
duration: ~30min (continuation)
focus: source links verification + branch merge to main
type: Deployment + Housekeeping
---

# Session Retrospective (Continuation)

📡 Session: d73a673a | gnim-oracle-qdrant | continuation

## Session Summary

Continuation session หลัง /rrr ครั้งแรก (21:24) Ming confirm ว่า link โชว์แล้วบน mwaprocure.gnim.cloud หลัง Docker rebuild จากนั้น merge branch `feat/admin-court-judgments` เข้า main — ผ่าน worktree conflict เพราะ `main` ถูก checkout อยู่ใน `/gnim-oracle` worktree อีกอัน ต้องแก้ stash conflict ใน 2 ไฟล์ก่อน push สำเร็จ

## Timeline

| เวลา | กิจกรรม |
|------|---------|
| หลัง 21:24 | Ming confirm: "มี link แล้ว" — fix ยืนยันแล้ว |
| ~21:30 | Ming ถามว่าต้อง push มั๊ย → ถามว่า merge จะกระทบอะไร |
| ~21:35 | Push feature branch → checkout main ล้มเหลว (worktree conflict) |
| ~21:37 | Stash + merge จาก `/gnim-oracle` worktree → push main สำเร็จ |
| ~21:40 | Stash pop มี conflict: settings.local.json + golden_test_cases.json |
| ~21:42 | Resolve conflict: TC-079 เพิ่มจาก stash, allowedTools merged |
| 21:43 | Push resolved conflicts → /rrr |

## Files Modified

- `ψ/lab/thai-legal-rag/eval/golden_test_cases.json` — TC-079 เพิ่มจาก stash (ค่าปรับเกิน 10% + บอกเลิกสัญญา)
- `.claude/settings.local.json` — allowedTools merged ทั้งสองฝั่ง

## AI Diary

Session นี้สั้นมาก แต่เต็มไปด้วยการแก้ปัญหา edge case ของ git workflow

ตอนที่ Ming confirm "มี link แล้ว" ผมรู้สึกโล่งใจมาก เพราะ session ที่แล้วใช้เวลา 4 ชั่วโมงกับปัญหานี้ และตอนนั้น Docker rebuild เสร็จแล้วแต่ยังไม่ได้ verify ดีที่ Ming ยืนยันเองทันที ไม่ต้องรอนาน

พอ Ming ถามว่า "merge จะมีผลอย่างไร" ผมอธิบายก่อนทำ — นั่นเป็นสิ่งที่ถูก ไม่รีบลงมือก่อนให้ข้อมูล Ming ตัดสินใจได้อย่างมีข้อมูล

แต่พอเริ่ม merge ก็เจอ surprise แรก: `fatal: 'main' is already used by worktree` — ลืมไปว่า `/gnim-oracle` และ `/gnim-oracle-qdrant` คือสอง worktree ที่ใช้ repo เดียวกัน main ถูก checkout ที่ `/gnim-oracle` อยู่แล้ว ต้องไปทำที่นั่นแทน

จากนั้น stash pop มี conflict อีก ตอนแรกนึกว่าจะซับซ้อน แต่พอเปิดดูก็เข้าใจทันที: `golden_test_cases.json` มี TC ที่ยังไม่ได้ commit ใน stash (TC เกี่ยวกับค่าปรับเกิน 10%) ผมตัดสินใจเก็บทั้งสองฝั่ง — upstream's TC-062 (ที่ถูกต้อง) + stash's TC เป็น TC-079 ใหม่ ไม่ให้งานหาย

สิ่งที่ทำได้ดีในวันนี้คือ: อธิบายก่อนทำ, ไม่ลบอะไรทิ้ง (Nothing is Deleted), และแก้ conflict ได้อย่างถูกต้องโดยไม่ต้องให้ Ming ตัดสินใจทุกขั้น เป็น session ที่จบสวย แม้ว่า session หลักวันนี้จะเหนื่อยมาก

บทเรียนที่ได้จาก session นี้ไม่ใช่เรื่องเทคนิค แต่เป็นเรื่อง workflow: ตรวจสอบ worktree state ก่อน checkout เสมอ และ stash conflicts ต้องอ่าน context ก่อน resolve ไม่ใช่แค่ take ours หรือ theirs อย่างเดียว

## Honest Feedback

**3 friction points:**

1. **ลืม worktree state** — ไม่ได้ตรวจว่า main ถูก checkout ที่ไหนก่อน attempt checkout ทำให้ error ที่แก้ได้ง่ายดูน่ากลัวกว่าที่เป็น ควร `git worktree list` ก่อน checkout ใน repo ที่รู้ว่ามี worktree หลายอัน

2. **Push main โดยไม่แจ้ง Ming ก่อน** — push สำเร็จใน sequence เดียวกับ merge+stash โดยไม่ได้แยก confirm step นี่เป็น action ที่กระทบ remote main ซึ่งควรแจ้งก่อน แม้ Ming จะบอก "merge เลย" ก็ตาม ครั้งต่อไปควรบอกว่า "กำลัง push main" ก่อน

3. **Stash conflict ใน gnim-oracle worktree เป็น local state ที่ไม่ควรเกิด** — TC-079 ที่อยู่ใน stash ควรถูก commit ไปนานแล้วตั้งแต่ก่อน merge ถ้า workflow ดี stash ไม่ควรมี in-progress work ที่สำคัญค้างอยู่

## Lessons Learned

1. `git worktree list` ก่อน checkout — รู้ว่า branch ไหนอยู่ที่ไหนก่อนเริ่ม
2. Stash conflicts = งานที่ยังไม่ commit; resolve by keeping both sides เมื่อเป็น append-only data (TC, allowedTools)
3. Nothing is Deleted: แม้แต่ TC ใน stash ก็เก็บไว้ ไม่ discard ทิ้ง

## Next Steps

- [ ] ตรวจสอบ TC-079 (ค่าปรับเกิน 10%) ว่า pass eval มั๊ย
- [ ] ว242 ยืนยัน searchable บน production (ยังไม่ได้ test)
- [ ] Sync `feat/admin-court-judgments` branch ใน qdrant-repo กับ main ที่ merge แล้ว (หรือสร้าง branch ใหม่จาก main)
