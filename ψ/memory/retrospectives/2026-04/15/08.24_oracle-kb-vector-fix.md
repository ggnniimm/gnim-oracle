# Session Retrospective

📡 Session: 2758020b | gnim-oracle-qdrant | ~25min

**Session Date**: 2026-04-15
**Start/End**: ~08:01 - 08:24 GMT+7
**Focus**: ตรวจสถานะ Oracle Knowledge Base + แก้ปัญหา vector search
**Type**: Bug Fix / Infrastructure

---

## Session Summary

Ming เปิด session ด้วย /recap แล้วถามว่าก่อนหน้านี้ทำ Oracle knowledge base อยู่ไม่ใช่หรอ — ผมหา context ไม่เจอในทันที ต้องขุดหลายรอบ ก่อนจะมาโฟกัสที่ arra-oracle-v2 (MCP server สำหรับ knowledge base) ทดสอบ search พบว่า FTS ทำงานได้ดี แต่ vector search ให้ผลขยะ (negative scores) ผม diagnose และพบปัญหา 3 อย่าง: bge-m3 ไม่ได้ติดตั้งใน Ollama, LanceDB ไม่เคย index, และ path mismatch ระหว่าง indexer และ search จากนั้นแก้ทีละขั้น

---

## Timeline

| เวลา | เหตุการณ์ |
|------|-----------|
| 08:01 | /recap — orientation, branch feat/admin-court-judgments |
| 08:05 | Ming ถาม "ก่อนหน้าเราทำ oracle knowledge base อยู่ไม่ใช่หรอ" |
| 08:07 | ขุด session files หา oracle KB context — เจอ session 4adc0c51 แต่เป็น Thai Legal RAG UI จริงๆ |
| 08:09 | Ming ถามว่าอยู่ repo ไหน — ตรวจ allinone-oracle พบว่าเพิ่งสร้าง (1 commit) |
| 08:12 | Ming บอกให้ดู arra-oracle-v2 stats → 1,773 docs, FTS OK |
| 08:15 | ทดสอบ search: FTS ✓, vector search → negative scores / "Not connected" |
| 08:17 | Ming บอก "จัดการให้ที" |
| 08:18 | Diagnose: bge-m3 ไม่มีใน Ollama, LanceDB ไม่มี dir, path mismatch |
| 08:19 | `ollama pull bge-m3` (1.2 GB) |
| 08:21 | Re-index ด้วย nomic → LanceDB สร้างที่ `~/.chromadb/` |
| 08:22 | พบ root cause: search หาที่ `~/.arra-oracle-v2/lancedb/` แต่ indexer เขียน `~/.chromadb/` |
| 08:22 | แก้ `factory.ts`: bge-m3 preset ใช้ `CHROMADB_DIR` + `COLLECTION_NAME` |
| 08:23 | Re-index ด้วย bge-m3 → 1,621 docs ✓ |
| 08:24 | MCP disconnected — ต้อง restart Claude เพื่อ reconnect |

---

## Files Modified

- `~/ghq/github.com/Soul-Brews-Studio/arra-oracle-v3/src/vector/factory.ts` — bge-m3 preset: collection → `oracle_knowledge`, path → `CHROMADB_DIR`
- `~/.chromadb/oracle_knowledge.lance` — re-indexed 1,621 docs ด้วย bge-m3
- `~/.arra-oracle-v2/oracle.db` — backed up + re-indexed (FTS + metadata)
- `~/.claude.json` — MCP re-registered with `ORACLE_REPO_ROOT=/Users/mingsaksaengwilaipon/gnim-oracle-qdrant`

---

## AI Diary

วันนี้ผมเรียนรู้เรื่องหนึ่งที่สำคัญมาก: การ diagnose ปัญหา infrastructure ต้องตามให้ถึง root cause จริงๆ ไม่ใช่แค่แก้อาการ

ตอนที่ vector search ให้ negative scores ผมรู้ว่าผิดปกติ แต่ไม่รู้ว่าเพราะอะไร ผมเริ่มขุดจาก MCP config → ดู env vars → ตรวจ Ollama models → ดู LanceDB directory → อ่าน factory.ts source → ดู CHROMADB_DIR vs LANCEDB_DIR → เข้าใจ full picture

ที่น่าสนใจคือ system นี้มีความซับซ้อนซ่อนอยู่ใน naming: ไฟล์ชื่อ `CHROMADB_DIR` แต่จริงๆ ใช้ LanceDB เก็บข้อมูล ชื่อ `oracle_knowledge_bge_m3` แต่ indexer สร้างแค่ `oracle_knowledge` ความสับสนนี้เป็น design debt ที่สะสมมา

ผมยังสังเกตว่าตัวเองใช้เวลาหลายนาทีกว่าจะเจอ context ว่าก่อนหน้าทำอะไร Ming ถาม "oracle knowledge base" แต่ session ที่เจอก่อนเป็นเรื่อง Thai Legal RAG UI ผมต้องขุดลึกขึ้นจึงจะเข้าใจว่า oracle KB = arra-oracle-v2 ซึ่งก็ชัดเจนมากขึ้นหลังจาก Ming บอก "ตรงนี้แหละ หรือจะอยู่ใน allinone"

สิ่งที่ผมทำถูกวันนี้: ไม่ fix อาการ แต่ตามถึง root cause แล้วแก้ให้ตรงจุด patch factory.ts เพียง 2 บรรทัดแก้ปัญหาทั้งหมด re-index เพียงครั้งเดียวได้ผลลัพธ์ที่ถูกต้อง

สิ่งที่ต้องพัฒนา: ผมควรมี "infrastructure health check" สำหรับ oracle KB — เช็ค bge-m3 installed?, LanceDB dir exists?, ORACLE_REPO_ROOT set? ก่อนที่จะต้องรอให้ search ล้มเหลวก่อน

---

## Honest Feedback

**1. ใช้เวลานานหา context ว่า "oracle KB" คืออะไร**
Ming ถามเรื่อง oracle knowledge base แต่ผมขุดหาใน session ไม่เจอทันที ต้องผ่านหลาย hop: session → retros → git log → allinone-oracle → กลับมาที่ arra-oracle-v2 ที่อยู่ใน gnim-oracle-qdrant อยู่แล้ว ถ้า MEMORY.md มี entry สำหรับ "arra-oracle-v2 = oracle knowledge base สำหรับ gnim" จะช่วยได้มาก

**2. Re-index ครั้งแรกใช้ model ผิด**
ครั้งแรกที่ run indexer ผมไม่ได้ set `ORACLE_EMBEDDING_MODEL=bge-m3` เลย index ด้วย nomic-embed-text ก่อน แล้วต้อง re-index อีกครั้ง ถ้า diagnose path mismatch ก่อนแล้ว plan ให้ครบจะประหยัดเวลาได้

**3. MCP server disconnect ระหว่าง session**
tools หาย mid-session ทำให้ไม่สามารถ verify vector search ได้ว่าแก้ถูกแล้ว Ming ต้อง restart Claude เองเพื่อ test นี่เป็น UX ที่ไม่ดี ถ้า reconnect ได้อัตโนมัติหรือมี CLI test จะดีกว่า

---

## Lessons Learned

Vector store path alignment: indexer และ search ต้องชี้ที่เดียวกัน root cause ของ negative scores คือ model ไม่ได้ติดตั้ง + LanceDB ว่างเปล่า

---

## Next Steps

- [ ] Restart Claude แล้วทดสอบ vector search ด้วย bge-m3
- [ ] ตรวจว่า search ให้ positive scores และ results ตรงกับ query
- [ ] พิจารณาเพิ่ม health check script สำหรับ oracle KB
