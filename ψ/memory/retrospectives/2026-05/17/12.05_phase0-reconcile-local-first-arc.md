---
session: a3832748
date: 2026-05-17
time: 12:05 GMT+7
repo: thai-legal-rag
focus: Phase 0 — prod snapshot + reconcile + commit (Local-first arc)
type: Refactoring / Architecture
---

# Session Retrospective — 2026-05-17

📡 Session: a3832748 | thai-legal-rag | ~4h (multi-day carry-over)

## Session Summary

เซสชั่นนี้เป็น Phase 0 ของแผน Local-first (`compiled-dazzling-marshmallow.md`) — เป้าหมายคือดึง stranded code ทั้งหมดจาก prod เข้า git ให้ครบ ก่อนเริ่ม Phase 1 (build mechanism) ใน session ถัดไป

ก่อนหน้านี้ (จากการ compact) ได้ทำ:
- Local Qdrant rebuild สำเร็จ: 31,927 chunks (สะอาด ไม่ double-indexed)
- วิเคราะห์ความต่าง eval/run_eval.py (local 414 บรรทัด) vs pipeline/run_eval.py (prod 351 บรรทัด)
- สร้างแผน Local-first 3-phase เต็ม
- Snapshot prod `/app/` → `prod-snapshot/` ผ่าน ssh+tar

Session นี้ (post-compact) ทำ commit สุดท้ายของ Phase 0:
- copy prod-only files → local `pipeline/`
- git add + commit: `3c0f72a`

## Timeline

| เวลา | Action |
|------|--------|
| (pre-compact) | Local Qdrant rebuild 31,927 chunks |
| (pre-compact) | Prod snapshot via ssh docker exec tar |
| (pre-compact) | Diff analysis: 8 files differ, 11 prod-only, 4 local-only |
| (pre-compact) | สร้าง RECONCILE_2026-05-17.md + แผน Local-first |
| post-compact | Copy 6 prod-only files → pipeline/ |
| 12:05 | Commit `3c0f72a` — Phase 0 complete |

## Files Modified

| File | Action |
|------|--------|
| `.gitignore` | เพิ่ม `prod-snapshot/` |
| `RECONCILE_2026-05-17.md` | NEW — decision log |
| `pipeline/run_eval.py` | NEW — จาก prod (351 บรรทัด) |
| `pipeline/build_bm25_index.py` | NEW — จาก prod |
| `pipeline/rebuild_faiss_index.py` | NEW — จาก prod |
| `pipeline/patch_metadata.py` | NEW — จาก prod |
| `pipeline/golden_test_cases.json.bak.2026-04-30` | NEW — historical backup |
| `pipeline/golden_test_cases.json.bak.2026-04-30-tc044` | NEW — historical backup |

## AI Diary

วันนี้รู้สึกเหมือนกำลังสะสางงานคั่งค้างมาหลายเดือน — การ snapshot prod แล้ว diff กับ local ทำให้เห็นชัดว่า drift นั้นหนักกว่าที่ทุกคนคิด

ที่น่าตกใจที่สุดคือ `src/ingestion/ocr.py` — local มี 1,246 บรรทัด แต่ prod มีแค่ 651 บรรทัด (ต่างกัน 595 บรรทัด!) นั่นหมายความว่า OCR pipeline ที่พัฒนามาอย่างหนักใน local ไม่เคยถูก deploy ขึ้น prod เลย เป็นเวลา 5+ สัปดาห์ Ming ทำงานบน local โดยคิดว่า prod ตามทัน แต่จริงๆ แล้ว prod ยังใช้ของเก่าอยู่

ความรู้สึกตอนทำ reconcile คือ "เราต้องทำแบบนี้ให้ถูกต้อง" — ไม่ใช่แค่ commit ทุกอย่างเข้าไป แต่ต้องตัดสินใจแต่ละไฟล์: อะไรคือ canonical? อะไรคือ dead code? อะไรคือ stranded แต่ยังมีคุณค่า? การทำ RECONCILE_2026-05-17.md เป็น decision log ทำให้กระบวนการนี้โปร่งใส — ถ้าในอนาคตสงสัยว่าทำไม `src/generator.py` (orphan) ถึงไม่อยู่ใน git ก็มีคำตอบอยู่แล้ว

สิ่งที่ยากที่สุดในเซสชั่นนี้ไม่ใช่ technical — แต่คือการยอมรับว่า process ที่ผ่านมาพัง ที่มีทั้ง `pipeline/run_eval.py` (prod) กับ `eval/run_eval.py` (local) ที่ diverge กัน ทั้งสองตัวมีฟีเจอร์คนละอย่างที่อีกตัวไม่มี — นั่นแสดงว่า Ming พัฒนาสองสายขนานกันโดยไม่รู้ตัว การมี Local-first model จะป้องกันไม่ให้เกิดแบบนี้อีก: มี canonical source เดียว, deploy direction ชัด, ไม่มี "ใครเป็นพ่อ" อีกต่อไป

การ compile แผน 3-phase ทำให้ session ถัดๆ ไปชัดขึ้นมาก — Phase 1 (build scripts) คือสิ่งที่ต้องทำต่อ และตอนนี้มีฐานที่มั่นคง: git มีทุกอย่างที่ควรมีแล้ว

## Honest Feedback

**Friction 1: Session compaction ทำให้เสีย momentum**

ก่อน compact กำลังจะ commit แต่ context หมด ต้องเริ่มใหม่จาก summary — ดีที่ summary ละเอียด แต่ก็ยังมีช่วง "ต้องหาตำแหน่ง" ก่อนจะเริ่มได้ถ้า session ยาวขนาดนี้ควรทำ checkpoint commit บ่อยกว่านี้

**Friction 2: Quota exhaustion block eval**

ตลอด session เจอ 429 RESOURCE_EXHAUSTED บน Vertex AI embedding quota เมื่อ local Qdrant rebuild ทำพร้อมกับ prod serving users ทำให้ไม่สามารถ verify local eval ได้ — ต้องรอ/ข้ามไป การแชร์ quota ระหว่าง local dev กับ prod runtime คือ friction ที่จะเกิดซ้ำจนกว่า Phase 2 (CI quota isolation) จะ live

**Friction 3: ความซับซ้อนของ decision matrix**

การ diff 19+ ไฟล์แล้วตัดสินใจแต่ละตัวต้องใช้เวลามาก และง่ายต่อการพลาด (เช่น `golden_test_cases.json` ที่ต้องตรวจก่อนว่าตรงกับ local หรือไม่) ถ้ามี `scripts/diff_prod.sh` ที่ automated decision logic ตั้งแต่แรก process นี้จะเร็วกว่านี้มาก — Phase 3 จะสร้าง `drift_check.sh` เพื่อป้องกันไม่ให้ต้องทำแบบ manual อีก

## Lessons Learned

ดู `2026-05-17_reconcile-before-rebuild.md`

## Next Steps

- **Phase 1a** (session ถัดไป): Pin Qdrant version, update Dockerfile (เพิ่ม `COPY pipeline/`), update `.dockerignore`
- **Phase 1b**: สร้าง `scripts/build.sh`, `snapshot_export.sh`, `deploy_image.sh`, `deploy_data.sh`
- **Phase 1c**: Manual deploy test — build image จาก local, push ขึ้น prod, run smoke test
- **DEPLOY.md**: เขียน golden rules + step-by-step workflow
- **Local eval**: รัน `eval/run_eval.py` เมื่อ quota ฟื้น (target 82/84 บน local 31,927 chunks)
- **TC-067/074**: Monitor ก่อน fix (per feedback_verify_before_fix)
