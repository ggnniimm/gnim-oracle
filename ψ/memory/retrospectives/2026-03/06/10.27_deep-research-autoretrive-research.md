# Session Retrospective

**Session Date**: 2026-03-06
**Time**: ~07:30 - 10:27 GMT+7
**Focus**: Index rebuild + MQTT fix + Deep Research auto-retrieve + RAG research
**Type**: Bug Fix / Feature / Research

## Session Summary

Session เริ่มจาก continuation ของ index rebuild ที่ fail เมื่อวาน → rebuild สำเร็จ (27,237 chunks) → commit + push → /rrr (ล้มเหลว 3 ครั้ง lesson learned) → /deep-research fail เพราะ MQTT infra → แก้ mosquitto config + extension URL → Deep Research สำเร็จ → แก้ deep-research script ให้ auto-retrieve ผ่าน AppleScript → research 3 เรื่อง: Knowledge Graph, Gemini Embedding, RAG Deployment

## Timeline

1. Index rebuild สำเร็จ — 1,269 files → 27,237 chunks (~4 นาที)
2. Commit `e509383` + `a2a7877`, push to main
3. /rrr ล้มเหลว 3 ครั้ง → lesson "write immediately"
4. /deep-research fail → แก้ mosquitto.conf + extension MQTT_URL
5. Deep Research #1: Knowledge Graph สำหรับกฎหมายไทย
6. Deep Research #2: Gemini Embedding ใน RAG — พบว่า text-embedding-004 ถูกปิดแล้ว
7. แก้ deep-research.ts เพิ่ม step 5: AppleScript poll + auto-retrieve
8. Deep Research #3: การ deploy ระบบ RAG — auto-retrieve ทำงานสำเร็จ (21 วินาที)

## Files Modified

- `/opt/homebrew/etc/mosquitto/mosquitto.conf` — สร้างใหม่
- `~/Downloads/claude-browser-proxy-main/background.js` — MQTT_URL → localhost
- `~/.claude/skills/deep-research/scripts/deep-research.ts` — เพิ่ม auto-retrieve via AppleScript

## AI Diary

Session นี้เป็น session ที่ผมเรียนรู้มากที่สุดในรอบหลายวัน ไม่ใช่เรื่อง code แต่เรื่อง "ทำเลย อย่าถาม" Ming ถามตรงๆ ว่า "ทำไมต้องมาปวดหัวเรื่อง MQTT ทุกที" — คำถามนี้ผลักให้ผมแก้ปัญหาที่ root cause: mosquitto auto-start + extension URL fix ไม่ใช่แค่ workaround ชั่วคราว

แต่สิ่งที่ภูมิใจที่สุดคือ deep-research auto-retrieve ที่ Ming ถามว่า "ทำยังไงให้ผลมาเลย โดยไม่ต้องบอก" ผมเพิ่ม AppleScript polling loop เข้าไปใน script — ตอนนี้สั่ง /deep-research แล้วผลมาเองเลย ไม่ต้อง copy-paste ไม่ต้อง "Gemini เสร็จแล้ว" ไม่ต้องทำอะไรอีก ทำงานได้ใน 21 วินาทีหลัง research เสร็จ

Research ที่ได้กลับมาก็มีค่ามาก: text-embedding-004 ถูก deprecate ไปแล้ว ต้องย้ายไป gemini-embedding-001 ซึ่งมี task_type parameter ที่เราไม่ได้ใช้ และ batch size เพิ่มได้จาก 100 เป็น 250 แก้ rate limit ที่เจอเมื่อวาน FAISS ที่เราใช้อยู่มีข้อจำกัดเรื่อง CRUD — ต้อง rebuild ทั้ง index ทุกครั้ง ซึ่งตรงกับปัญหาที่เจอพอดี

สิ่งที่ต้องปรับปรุง: polling script จับ tab สุดท้าย ถ้ามีหลาย Gemini tabs อาจจับ tab ผิด ควร track tabId จากตอน create

## Honest Feedback

1. **Deep Research script ดึงผล tab ผิดครั้งแรก**: Poll script จับ "tab สุดท้าย" แต่ผลที่ได้เป็นของ tab เก่า (knowledge graph แทน embedding) เพราะ tab ใหม่ยังอยู่หน้า plan Gemini ยังไม่มี message-content ทำให้ fallback ไป tab เก่าที่มีผลอยู่แล้ว ควรเก็บ tab URL หรือ index ตอน create แล้ว poll เฉพาะ tab นั้น

2. **ไม่ได้ตรวจ embedding model ทันทีหลังได้ผล research**: Gemini บอกว่า text-embedding-004 ถูกปิดแล้ว แต่ผมไม่ได้เช็คว่าระบบเราใช้ model ไหนอยู่ ควรทำทันทีเมื่อได้ข้อมูลที่ actionable

3. **Session ยาวเกินไป 3 ชั่วโมง ทำหลายเรื่องเกินไป**: Index rebuild + MQTT fix + script improvement + 3 research topics ทำให้แต่ละเรื่องไม่ได้ follow-up ทันที ควรแบ่ง session ให้เล็กลง focus เรื่องเดียว

## Lessons Learned

- Deep Research auto-retrieve: AppleScript + Chrome JavaScript = workaround ที่ reliable เมื่อ extension scripting ไม่ทำงาน
- text-embedding-004 ถูก deprecate (ม.ค. 2026) → ต้องย้ายไป gemini-embedding-001
- task_type parameter (RETRIEVAL_DOCUMENT vs RETRIEVAL_QUERY) ช่วยเพิ่มความแม่นยำ
- FAISS ไม่เหมาะกับ data ที่เปลี่ยนบ่อย — Qdrant เป็นทางเลือกสำหรับ production

## Next Steps

- ตรวจ embedding model ที่ใช้อยู่ — ถ้ายัง text-embedding-004 ต้องเปลี่ยน
- เพิ่ม task_type parameter ใน faiss_store.py
- เพิ่ม batch size จาก 100 เป็น 250
- Run eval: verify 46/46 PASS
- Files 43, 44, 48/2561: รอ Ming
- Fix deep-research script: track specific tab instead of "last tab"
