---
installer: oracle-skills-cli v1.5.80
name: where-we-are
description: v1.5.80 L-SKLL | Session awareness - alias for /recap --now. Use when user asks "now", "where are we", "what are we doing", "status".
---

# /where-we-are

This skill has been merged into `/recap --now`.

## Instructions

Run `/recap --now` (or `/recap --now deep` if user asked for deep mode).

Pass arguments: `$ARGUMENTS`
